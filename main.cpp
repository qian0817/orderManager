#include "order_manager.hpp"

int main() {
//    list<order> l;
//    std::string s = "test";
//    l.add(order(1, s, s, 10, 10));
//    l.add(order(2, s, s, 10, 10));
//    for (auto &i:l) {
//        std::cout << i << std::endl;
//    }
    order_manager manager;
    manager.main_menu();
    return 0;
}
